config.json
config.json.*
# api_id and api_hash for a self-hosted Bot API server. These
# belong to the Telegram account, not just the bot.
.env
service/.env
chute.lock
state.json
state.json.tmp
staging/
chute.log
.DS_Store
.claude/settings.local.json
